{
  "name": "log",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run -r test && vp run -r build",
    "dev": "npx convex dev & vp run web#dev",
    "preview": "vercel env pull .env.production --environment=production --yes && vp run -r build && vp run web#preview",
    "build": "vp run -r build",
    "lint": "vp lint",
    "fmt": "vp fmt",
    "fmt:check": "vp fmt --check",
    "typecheck": "vp run -r typecheck",
    "check": "vp check",
    "prepare": "vp config"
  },
  "devDependencies": {
    "convex": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.0"
}
